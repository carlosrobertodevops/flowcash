@AGENTS.md

## Claude Code

- Keep this file thin; shared project rules belong in `AGENTS.md`.
- Start sessions with `caveman wenyan-ultra` and `context-mode` active.
- Run `graphify update .` only when the project/work is ready for final sync.
- Use `docs/UI-PARTNER.md` before changing UI.
- For path-specific or task-specific rules, prefer `.claude/rules/*.md` or skills instead of expanding this file.
